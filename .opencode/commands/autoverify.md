---
description: Run 137 verification checks
agent: build
---

Run the verification checks:
`pwsh -NoProfile -ExecutionPolicy Bypass -File vbe-auto/verify.ps1 -ConfigPath vbe-auto/config.json` (from project root)

Report the results, including any failures or warnings.
